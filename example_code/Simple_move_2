LOOP
	IF SENSOR FRONT PROX
		IF SENSOR LEFT PROX
			REPEAT 90
				RIGHT
		ELSE
			REPEAT 90
				LEFT
	ELSE
		IF NOT SENSOR LEFT PROX
			WHILE NOT CENTERED POSITION
				MOVE
			REPEAT 90
				LEFT
			REPEAT 5
				MOVE
		ELSE
			MOVE
