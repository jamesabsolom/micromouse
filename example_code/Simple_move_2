LOOP
	IF SENSOR FRONT PROX
		IF SENSOR LEFT PROX
			REPEAT 90
				RIGHT
		ELSE
			REPEAT 90
				LEFT
	ELSE
		IF NOT SENSOR LEFT PROX
			WHILE NOT SENSOR FRONT PROX
				MOVE
			REPEAT 90
				LEFT
		ELSE
			MOVE
